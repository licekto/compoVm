descriptor HTTPServer extends server {
	externally provides {
		default : {};
	}

        externally requires {
            default : {};
        }

	service create(url) {}
	service discard() {}

        constraint httpOnly() {}
}
